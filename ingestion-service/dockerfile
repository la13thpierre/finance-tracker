FROM node:20-slim
WORKDIR /app
COPY package.json ./
RUN npm install --omit=dev
COPY src ./src
RUN groupadd -r appgroup && useradd -r -g appgroup appuser
USER appuser
EXPOSE 4002
CMD ["node", "src/index.js"]